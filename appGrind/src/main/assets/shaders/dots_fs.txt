#version 300 es

in vec2 vPosition;

out vec4 outFragColor;

void main() {
    float dist = length(vPosition);
    outFragColor = vec4(vec3(0.6, 0.65, 0.80) * 0.5, smoothstep(1.0, 0.75, dist) * 1.0);
}
