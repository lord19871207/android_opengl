#version 300 es

uniform sampler2D sTexture;

in vec2 vTexPosition;

out vec4 outFragColor;

void main() {
    vec3 c = texture(sTexture, vTexPosition).rgb;
    float gray = dot(c, vec3(0.299, 0.587, 0.114));
    outFragColor = vec4(gray, gray, gray, 1.0);
}
