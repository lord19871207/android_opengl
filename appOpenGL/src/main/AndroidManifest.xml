<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.harism.opengl.app">

    <uses-feature
        android:glEsVersion="0x00030001"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".test.TestRenderActivity"
            android:label="@string/test_renderer_title"
            android:screenOrientation="portrait" />

        <activity
            android:name=".camera2.BasicCameraRenderActivity"
            android:label="@string/camera2_basic_renderer_title"
            android:screenOrientation="portrait" />

        <activity
            android:name=".camera2.RawCameraRenderActivity"
            android:label="@string/camera2_raw_renderer_title"
            android:screenOrientation="portrait" />

    </application>

</manifest>
